# TOOL CLIENT

Chạy code:

    + require: Node JS 18.16 [https://nodejs.org]

    + thêm tài khoản facebook vào file src/dummy/accounts.ts để chạy test (về sau sẽ dùng DB)

    + yarn / npm install

    + yarn start / npm start

    + Bật server và manager client